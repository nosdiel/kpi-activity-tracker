-- NiNi - KPI schema (schema_fixed.sql)
-- The pasted schema was truncated mid-stream by the chat tool.
-- ACTION REQUIRED: replace this file's contents with the full schema_fixed.sql,
-- then run:  npx supabase db push   (after `supabase link --project-ref gcplpjgpwxwezouhnkly`).

-- The truncated paste began with:
--   CREATE TYPE public.app_role AS ENUM ('admin','regional_manager','store_manager','super_admin');
--   CREATE TYPE public.sales_source AS ENUM ('square','manual','toast');
--   plus functions: can_access_location, handle_new_user, has_role, is_admin, etc.
--   and tables/policies for: profiles, user_roles, user_locations, weekly_pnl,
--   weekly_targets, vendor_contacts, and more.

-- Paste the full SQL here (verbatim), then `supabase db push`.
